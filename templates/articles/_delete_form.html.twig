<form method="post" action="{{ path('articles_delete', {'idlesports': article.idlesports}) }}" onsubmit="return confirm('êtes-vous sure de vouloir supprimer cette articles?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article.idlesports) }}">
    <button class="btn">Delete</button>
</form>
