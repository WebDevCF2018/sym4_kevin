{% extends 'template.html.twig' %}

{% block title %}Articles{% endblock %}

{% block content %}
    <h1>Articles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idlesports</th>
                <td>{{ article.idlesports }}</td>
            </tr>
            <tr>
                <th>Letitres</th>
                <td>{{ article.letitres }}</td>
            </tr>
            <tr>
                <th>Letextes</th>
                <td>{{ article.letextes }}</td>
            </tr>
            <tr>
                <th>Ladates</th>
                <td>{{ article.ladates ? article.ladates|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('articles_index') }}">Revenir à la liste</a>

    <a href="{{ path('articles_edit', {'idlesports': article.idlesports}) }}">Modifier</a>

    {{ include('articles/_delete_form.html.twig') }}
{% endblock %}
